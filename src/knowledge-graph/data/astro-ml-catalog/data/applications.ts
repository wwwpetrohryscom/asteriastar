import type { MlRecord } from "@/knowledge-graph/data/astro-ml-catalog/types";

/** Astronomical applications of machine learning. Each links to the REUSED domain entities it acts
 *  on (galaxy morphologies, transients, the transit method, lensing, redshift, the Rubin alert
 *  stream) and the ML methods it uses. */
const app = (r: Omit<MlRecord, "kind" | "id" | "sources"> & { slug: string; sources?: MlRecord["sources"] }): MlRecord => ({ sources: ["nasa"], ...r, kind: "application", id: `ml_application:${r.slug}` });

export const applications: MlRecord[] = [
  app({ slug: "galaxy-morphology-classification", name: "Galaxy Morphology Classification", relatedKeys: ["ml_method:classification", "galaxy_morphology:spiral", "galaxy_morphology:elliptical"], description: "Sorting galaxies by their shape — spiral, elliptical, irregular, merging — from survey images. One of the earliest large-scale meetings of astronomy and machine learning, building on the labels gathered by citizen-science projects like Galaxy Zoo to train automatic classifiers for surveys too large to inspect by eye.", sources: ["nasa"], highlights: ["Classifying galaxies at survey scale"] }),
  app({ slug: "supernova-classification", name: "Supernova Classification", relatedKeys: ["ml_method:classification", "transient_class:type-ia-supernova", "astronomy_method:spectral-classification", "alert_system:transient-name-server"], description: "Deciding what kind of exploding star a transient is — often from its light curve alone, before or without a spectrum. Fast, automatic classification is essential when a survey finds thousands of supernovae a night and only a few can be followed up in detail.", sources: ["nasa"] }),
  app({ slug: "photometric-redshifts", name: "Photometric Redshifts", altNames: ["photo-z"], relatedKeys: ["ml_method:regression", "cosmology_concept:redshift", "astronomy_method:photometry"], description: "Estimating how far away a galaxy is from its brightness in a few broad colour bands, without taking a full spectrum. It is far less precise than a spectroscopic redshift but can be done for the hundreds of millions of galaxies in an imaging survey, underpinning weak-lensing and large-scale-structure cosmology.", sources: ["nasa"], highlights: ["Redshifts for hundreds of millions of galaxies"] }),
  app({ slug: "transit-detection", name: "Transit Detection", relatedKeys: ["ml_method:classification", "exoplanet_detection_method:transit"], description: "Picking the tiny, periodic dips of an exoplanet transit out of a noisy stellar light curve — and telling a real planet from the many kinds of false positive. Machine learning now helps sift the enormous light-curve archives of transit surveys for the faintest candidates.", sources: ["nasa"] }),
  app({ slug: "strong-lens-finding", name: "Strong Lens Finding", relatedKeys: ["astronomy_method:gravitational-lensing", "ml_method:classification"], description: "Searching survey images for the rare, distinctive arcs and rings of strong gravitational lensing — where a foreground mass bends the light of a background galaxy. The lenses are rare enough, and the images numerous enough, that automated finders are the only way to build large samples.", sources: ["nasa"] }),
  app({ slug: "source-extraction", name: "Source Extraction", relatedKeys: ["astronomy_method:photometry", "ml_method:classification"], description: "Finding and measuring the individual stars and galaxies in an astronomical image, and separating real sources from noise and artefacts — the first step of nearly every imaging pipeline. Machine-learning methods increasingly complement the classical algorithms, especially in crowded or blended fields.", sources: ["nasa"] }),
  app({ slug: "real-time-alert-classification", name: "Real-Time Alert Classification", relatedKeys: ["observatory:vera-rubin-observatory", "alert_system:rubin-alert-stream", "open_science_practice:cross-matching", "ml_method:anomaly-detection"], description: "Classifying the flood of alerts that a survey like Rubin issues — millions each night when something on the sky changes — quickly enough to catch the fleeting events worth following up. It is the problem the community alert brokers exist to solve.", sources: ["nasa"], highlights: ["Millions of alerts a night, classified as they arrive"] }),
];
