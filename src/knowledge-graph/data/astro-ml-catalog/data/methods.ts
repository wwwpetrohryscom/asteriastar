import type { MlRecord } from "@/knowledge-graph/data/astro-ml-catalog/types";

/** Machine-learning methods used in astronomy. Each links to the applications it powers and the
 *  other methods it builds on. */
const met = (r: Omit<MlRecord, "kind" | "id" | "sources"> & { slug: string; sources?: MlRecord["sources"] }): MlRecord => ({ sources: ["nasa"], ...r, kind: "method", id: `ml_method:${r.slug}` });

export const methods: MlRecord[] = [
  met({ slug: "classification", name: "Classification", paradigmLabel: "Supervised", relatedKeys: ["ml_application:galaxy-morphology-classification", "ml_application:supernova-classification"], description: "Assigning objects to discrete categories from their measured features — star or galaxy, supernova type, real detection or artefact. It is the workhorse of survey astronomy, sorting the millions of objects a modern survey finds into kinds a human never could review one by one.", sources: ["nasa"], highlights: ["Sorting millions of objects into kinds"] }),
  met({ slug: "regression", name: "Regression", paradigmLabel: "Supervised", relatedKeys: ["ml_application:photometric-redshifts"], description: "Predicting a continuous quantity from an object's features — a galaxy's redshift, a star's temperature or age — rather than a discrete label. Regression turns cheap, plentiful measurements into estimates of quantities that would otherwise need expensive follow-up.", sources: ["nasa"] }),
  met({ slug: "clustering", name: "Clustering", paradigmLabel: "Unsupervised", relatedKeys: ["ml_method:anomaly-detection"], description: "Grouping objects by similarity without being told the categories in advance — letting the structure in the data reveal itself. It is used to find natural classes of objects and to organise catalogues too large to label by hand.", sources: ["nasa"] }),
  met({ slug: "representation-learning", name: "Representation Learning", relatedKeys: ["ml_method:self-supervised-learning", "ml_method:foundation-models"], description: "Learning a compact, informative summary — an 'embedding' — of a complex object like an image or a light curve, so that similar objects sit near each other. Good representations make every downstream task, from classification to search, easier and more accurate.", sources: ["nasa"] }),
  met({ slug: "self-supervised-learning", name: "Self-Supervised Learning", paradigmLabel: "Self-supervised", relatedKeys: ["ml_method:representation-learning", "ml_method:foundation-models"], description: "Training a model on unlabelled data by inventing a task it can grade itself on — predicting a hidden part of an image, or telling two views of the same object apart. It is powerful in astronomy, where raw data is abundant but expert labels are scarce.", sources: ["nasa"], highlights: ["Learning from abundant data without labels"] }),
  met({ slug: "foundation-models", name: "Foundation Models", relatedKeys: ["ml_method:self-supervised-learning", "ml_method:representation-learning", "open_science_practice:reproducibility-and-fair"], description: "Large models pre-trained on broad data that can then be adapted to many specific tasks with little extra training. In astronomy they are an emerging approach to building a single model that understands images, spectra, or light curves across many problems at once.", sources: ["nasa"] }),
  met({ slug: "anomaly-detection", name: "Anomaly Detection", paradigmLabel: "Unsupervised", relatedKeys: ["ml_method:clustering", "ml_application:real-time-alert-classification"], description: "Finding the rare objects that do not fit any known pattern — the outliers that may be the next new class of transient or a genuinely unexpected discovery. As surveys outgrow human inspection, anomaly detection is how the strangest objects get noticed at all.", sources: ["nasa"], highlights: ["Finding the objects nobody expected"] }),
];
