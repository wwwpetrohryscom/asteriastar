import type { CalculatorRecord } from "@/knowledge-graph/data/scientific-calculators-catalog/types";
import { ARCSEC_PER_RAD } from "@/lib/calculators/constants";

/** Photometry, distance & astrometry calculators. Reuses the HR diagram, parallax method, and the
 *  ICRS reference frame. Examples validate against standard textbook cases. */
const calc = (r: Omit<CalculatorRecord, "id" | "sources" | "category"> & { slug: string; sources?: CalculatorRecord["sources"] }): CalculatorRecord => ({ sources: ["nasa"], ...r, category: "observational", id: `scientific_calculator:${r.slug}` });

const rad = (deg: number) => (deg * Math.PI) / 180;

export const observational: CalculatorRecord[] = [
  calc({ slug: "absolute-magnitude", name: "Absolute Magnitude", formula: "M = m − 5 log₁₀(d / 10 pc)", resultUnit: "mag", resultLabel: "Absolute magnitude", variables: [{ symbol: "m", label: "Apparent magnitude", unit: "mag", default: 5 }, { symbol: "d", label: "Distance", unit: "pc", default: 10 }], compute: (v) => v.m - 5 * Math.log10(v.d / 10), example: { expected: 5.0, tol: 0.01, note: "m = 5 at 10 pc → M = 5" }, relatedKeys: ["stellar_physics_concept:hertzsprung-russell-diagram"], description: "A star's intrinsic brightness — the apparent magnitude it would have at a standard distance of 10 parsecs. Removes distance so stars can be compared on equal footing.", highlights: ["m = 5 at 10 pc → M = 5"] }),
  calc({ slug: "distance-modulus", name: "Distance Modulus", formula: "μ = 5 log₁₀(d) − 5", resultUnit: "mag", resultLabel: "Distance modulus", variables: [{ symbol: "d", label: "Distance", unit: "pc", default: 100 }], compute: (v) => 5 * Math.log10(v.d) - 5, example: { expected: 5.0, tol: 0.01, note: "100 pc → μ = 5" }, relatedKeys: ["astronomy_method:parallax"], description: "The difference between apparent and absolute magnitude, which encodes distance. A modulus of five corresponds to 100 parsecs; each five magnitudes multiply the distance tenfold.", highlights: ["100 pc → μ = 5"] }),
  calc({ slug: "parallax-distance", name: "Parallax Distance", formula: "d = 1 / p", resultUnit: "pc", resultLabel: "Distance", variables: [{ symbol: "p", label: "Parallax angle", unit: "arcsec", default: 0.1 }], compute: (v) => 1 / v.p, example: { expected: 10, tol: 0.01, note: "0.1″ → 10 pc" }, relatedKeys: ["astronomy_method:parallax"], description: "The most direct distance measurement: a star's distance in parsecs is the reciprocal of its annual parallax in arcseconds. A parallax of one arcsecond defines one parsec — but no star is that close.", highlights: ["0.1″ → 10 pc"] }),
  calc({ slug: "angular-diameter", name: "Angular Diameter", formula: "θ = 2 arctan(D / 2d)", resultUnit: "arcsec", resultLabel: "Angular diameter", variables: [{ symbol: "D", label: "Physical diameter", unit: "km", default: 3474.8 }, { symbol: "d", label: "Distance", unit: "km", default: 384400 }], compute: (v) => 2 * Math.atan(v.D / (2 * v.d)) * ARCSEC_PER_RAD, example: { expected: 1865, tol: 5, note: "The Moon → ~1865″ (0.52°)" }, relatedKeys: ["scientific_calculator:parallax-distance"], description: "How large an object of known size appears at a given distance. The Moon and the Sun span almost exactly the same half-degree from Earth — the coincidence that makes total solar eclipses possible.", highlights: ["The Moon: ~0.52°"] }),
  calc({ slug: "angular-separation", name: "Angular Separation", formula: "cos θ = sin δ₁ sin δ₂ + cos δ₁ cos δ₂ cos(α₁ − α₂)", resultUnit: "°", resultLabel: "Angular separation", variables: [{ symbol: "ra1", label: "RA 1", unit: "°", default: 0 }, { symbol: "dec1", label: "Dec 1", unit: "°", default: 0 }, { symbol: "ra2", label: "RA 2", unit: "°", default: 0 }, { symbol: "dec2", label: "Dec 2", unit: "°", default: 10 }], compute: (v) => {
    const c = Math.sin(rad(v.dec1)) * Math.sin(rad(v.dec2)) + Math.cos(rad(v.dec1)) * Math.cos(rad(v.dec2)) * Math.cos(rad(v.ra1 - v.ra2));
    return (Math.acos(Math.max(-1, Math.min(1, c))) * 180) / Math.PI;
  }, example: { expected: 10, tol: 0.01, note: "(0,0) & (0,+10°) → 10°" }, relatedKeys: ["reference_frame:icrs"], description: "The angle on the sky between two positions given by their right ascension and declination — the great-circle distance between two points on the celestial sphere.", highlights: ["Great-circle distance on the sky"] }),
];
