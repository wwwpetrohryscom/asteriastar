import type { CalculatorRecord } from "@/knowledge-graph/data/scientific-calculators-catalog/types";
import { ARCSEC_PER_RAD } from "@/lib/calculators/constants";

/** Telescope & instrument calculators. Reuses Hubble as a diffraction-limited example and links the
 *  instrument calculators to one another. Examples validate against standard optics results. */
const calc = (r: Omit<CalculatorRecord, "id" | "sources" | "category"> & { slug: string; sources?: CalculatorRecord["sources"] }): CalculatorRecord => ({ sources: ["nasa"], ...r, category: "instrument", id: `scientific_calculator:${r.slug}` });

export const instrument: CalculatorRecord[] = [
  calc({ slug: "angular-resolution", name: "Angular Resolution (Rayleigh)", altNames: ["Diffraction limit"], formula: "θ = 1.22 λ / D", resultUnit: "arcsec", resultLabel: "Angular resolution", variables: [{ symbol: "lambda", label: "Wavelength", unit: "nm", default: 550 }, { symbol: "D", label: "Aperture", unit: "mm", default: 100 }], compute: (v) => (1.22 * (v.lambda * 1e-9) / (v.D * 1e-3)) * ARCSEC_PER_RAD, example: { expected: 1.38, tol: 0.02, note: "100 mm at 550 nm → ~1.4″" }, relatedKeys: ["space_telescope:hubble-space-telescope"], description: "The finest detail a telescope can resolve, set by diffraction at its aperture — the Rayleigh criterion. Bigger apertures see finer detail; a 100 mm telescope resolves about 1.4 arcseconds in green light.", highlights: ["100 mm: ~1.4″"] }),
  calc({ slug: "magnification", name: "Telescope Magnification", formula: "M = f_telescope / f_eyepiece", resultUnit: "×", resultLabel: "Magnification", variables: [{ symbol: "ft", label: "Telescope focal length", unit: "mm", default: 1000 }, { symbol: "fe", label: "Eyepiece focal length", unit: "mm", default: 10 }], compute: (v) => v.ft / v.fe, example: { expected: 100, tol: 0.01, note: "1000 mm / 10 mm → 100×" }, relatedKeys: ["scientific_calculator:angular-resolution"], description: "The magnification of a telescope–eyepiece pair — the ratio of their focal lengths. Useful magnification is capped by the aperture and the atmosphere, not by the eyepiece alone.", highlights: ["1000 / 10 mm → 100×"] }),
  calc({ slug: "image-scale", name: "Image Scale", formula: "s = 206.265 × pixel(µm) / focal(mm)", resultUnit: "″/px", resultLabel: "Image scale", variables: [{ symbol: "pixel", label: "Pixel size", unit: "µm", default: 3.76 }, { symbol: "focal", label: "Focal length", unit: "mm", default: 1000 }], compute: (v) => (206.265 * v.pixel) / v.focal, example: { expected: 0.776, tol: 0.01, note: "3.76 µm at 1000 mm → 0.78″/px" }, relatedKeys: ["scientific_calculator:field-of-view"], description: "How much sky each camera pixel covers, from the pixel size and focal length. Matching the image scale to the seeing (roughly 1–2″ per pixel) is the key to sharp astrophotography.", highlights: ["3.76 µm / 1000 mm → 0.78″/px"] }),
  calc({ slug: "field-of-view", name: "Field of View", formula: "FOV = 3437.75 × sensor(mm) / focal(mm)", resultUnit: "arcmin", resultLabel: "Field of view", variables: [{ symbol: "sensor", label: "Sensor size", unit: "mm", default: 23.5 }, { symbol: "focal", label: "Focal length", unit: "mm", default: 1000 }], compute: (v) => (v.sensor / v.focal) * (ARCSEC_PER_RAD / 60), example: { expected: 80.8, tol: 0.5, note: "23.5 mm at 1000 mm → ~81′" }, relatedKeys: ["scientific_calculator:image-scale"], description: "The angular extent of sky a camera frames, from the sensor size and focal length. A shorter focal length or a larger sensor takes in more sky.", highlights: ["23.5 mm / 1000 mm → ~1.3°"] }),
  calc({ slug: "limiting-magnitude", name: "Limiting Magnitude", formula: "m_lim ≈ 2.7 + 5 log₁₀(D_mm)", resultUnit: "mag", resultLabel: "Limiting magnitude", variables: [{ symbol: "D", label: "Aperture", unit: "mm", default: 100 }], compute: (v) => 2.7 + 5 * Math.log10(v.D), example: { expected: 12.7, tol: 0.1, note: "100 mm → ~12.7 mag" }, relatedKeys: ["scientific_calculator:angular-resolution"], description: "A rule-of-thumb estimate of the faintest star an aperture can show under dark skies. Approximate — real limits depend on sky brightness, magnification, and the observer — so it is offered as a guide, not a guarantee.", highlights: ["100 mm: ~12.7 mag (approx.)"] }),
  calc({ slug: "shot-noise-snr", name: "Photon Shot-Noise SNR", formula: "SNR = √N", resultUnit: "", resultLabel: "Signal-to-noise ratio", variables: [{ symbol: "N", label: "Source photons detected", unit: "e⁻", default: 10000 }], compute: (v) => Math.sqrt(v.N), example: { expected: 100, tol: 0.01, note: "10,000 photons → SNR 100" }, relatedKeys: ["scientific_calculator:limiting-magnitude"], description: "The best signal-to-noise achievable when the only noise is the Poisson statistics of the photons themselves — the shot-noise limit. To double the signal-to-noise you must collect four times as many photons.", highlights: ["Quadruple photons to double SNR"] }),
];
