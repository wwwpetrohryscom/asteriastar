import type { AssistantRecord } from "@/knowledge-graph/data/scientific-assistant-catalog/types";

/** Assistant capabilities. Grounded capabilities are backed by real retrieval over the graph today;
 *  architecture capabilities are interfaces for a future LLM layer. Each links to the reused example
 *  entities it works over and to sibling capabilities. */
const grounded = (r: Omit<AssistantRecord, "id" | "sources" | "grounding"> & { slug: string; sources?: AssistantRecord["sources"] }): AssistantRecord => ({ sources: ["nasa"], ...r, grounding: "grounded", id: `assistant_capability:${r.slug}` });
const arch = (r: Omit<AssistantRecord, "id" | "sources" | "grounding"> & { slug: string; sources?: AssistantRecord["sources"] }): AssistantRecord => ({ sources: ["nasa"], ...r, grounding: "architecture", id: `assistant_capability:${r.slug}` });

export const capabilities: AssistantRecord[] = [
  // Grounded — real retrieval over the graph today
  grounded({ slug: "scientific-search", name: "Scientific Search", capability: "Grounded search over the graph", relatedKeys: ["graph_view:graph-search", "assistant_capability:related-concepts"], description: "Semantic, scientific search across every entity in the knowledge graph — a direct route from a question to the real objects, concepts, missions, and people that answer it. Grounded in the graph; it returns only what is really there.", highlights: ["From a question to real entities"] }),
  grounded({ slug: "object-explanation", name: "Object Explanation", capability: "Grounded entity explanation", relatedKeys: ["astronomical_theory:keplers-laws", "assistant_capability:scientific-summaries"], description: "A grounded explanation of any entity — what it is, how it connects to the rest of the graph, and where the knowledge comes from — assembled entirely from the entity's own description, its relations, and its cited sources.", highlights: ["What it is, and where the knowledge comes from"] }),
  grounded({ slug: "concept-comparison", name: "Concept Comparison", capability: "Grounded shared-connection comparison", relatedKeys: ["planet:mars", "planet:venus"], description: "Compares two concepts by the real common ground between them — the entities they both connect to in the graph. Mars and Venus, for instance, share atmospheric escape, climate evolution, and their place in the Solar System. A comparison built from relations, not rhetoric.", highlights: ["Common ground, drawn from real relations"] }),
  grounded({ slug: "relationship-explanation", name: "Relationship Explanation", capability: "Grounded relation lookup", relatedKeys: ["assistant_capability:object-explanation", "assistant_capability:evidence-chains"], description: "Explains how two entities are related by naming the real relations that link them — 'discovered by', 'orbits', 'is a kind of' — so a connection is never asserted without the graph edge that backs it.", highlights: ["Every connection has a real edge behind it"] }),
  grounded({ slug: "evidence-chains", name: "Evidence Chains", capability: "Grounded shortest-path evidence", relatedKeys: ["astronomer:edwin-hubble", "cosmology_concept:dark-energy"], description: "Traces the chain of real relations connecting a claim to its supporting entities — for example, from Edwin Hubble through the expansion of the universe to dark energy. Every link in the chain is a genuine edge in the graph, so the reasoning can be followed and checked.", highlights: ["Reasoning you can follow, link by link"] }),
  grounded({ slug: "provenance-aware-answers", name: "Provenance-Aware Answers", capability: "Grounded, source-attributed answers", relatedKeys: ["open_science_practice:persistent-identifiers", "assistant_capability:citation-aware-answers"], description: "Every grounded answer carries the provenance of the entities it draws on — the sources cited on each, and the review status of the knowledge — so the reader can weigh how far to trust it. No answer without its provenance.", highlights: ["No answer without its provenance"] }),
  grounded({ slug: "citation-aware-answers", name: "Citation-Aware Answers", capability: "Grounded, citation-linked answers", relatedKeys: ["open_science_practice:the-ads-literature-service", "assistant_capability:provenance-aware-answers"], description: "Answers linked back to the literature and catalogues behind them, through the platform's persistent identifiers and the ADS literature service. A grounded assistant that shows its references.", highlights: ["Answers that show their references"] }),
  grounded({ slug: "related-concepts", name: "Related Concepts", capability: "Grounded neighbourhood recommendations", relatedKeys: ["stellar_process:main-sequence-evolution", "assistant_capability:reading-recommendations"], description: "Suggests the concepts most closely related to any entity — its real neighbours in the graph. Ask about the main sequence and it surfaces the CNO cycle, the HR diagram, and stellar structure, because those are the entities it actually connects to.", highlights: ["Real neighbours, not guesses"] }),
  grounded({ slug: "reading-recommendations", name: "Reading Recommendations", capability: "Grounded next-steps from the graph", relatedKeys: ["assistant_capability:related-concepts", "assistant_capability:learning-path-generation"], description: "Recommends what to read next from the platform's own entries, following the real relations outward from where a reader is — a grounded path deeper into a subject.", highlights: ["Where to go next, from the graph"] }),
  grounded({ slug: "scientific-summaries", name: "Scientific Summaries", capability: "Grounded summaries from entity data", relatedKeys: ["assistant_capability:object-explanation", "astronomical_theory:keplers-laws"], description: "Concise summaries assembled from the entities' own curated descriptions and their key relations — a faithful digest of what the graph holds, with nothing added.", highlights: ["A faithful digest, nothing added"] }),
  grounded({ slug: "learning-path-generation", name: "Learning-Path Generation", capability: "Grounded learning paths", relatedKeys: ["assistant_capability:reading-recommendations", "assistant_capability:related-concepts"], description: "Assembles a learning path through a subject from the platform's curated paths and the real dependencies between concepts — a grounded curriculum drawn from the graph, ordered from foundations to frontier.", highlights: ["A curriculum from the graph"] }),
  grounded({ slug: "cross-domain-reasoning", name: "Cross-Domain Reasoning", capability: "Grounded cross-domain links", relatedKeys: ["constellation:orion", "assistant_capability:evidence-chains"], description: "Reasons across the graph's domains — following a constellation from its stars to the myths named for it — using the real relations that bridge science, culture, and history. Connections across domains, never invented.", highlights: ["Bridges across science, culture & history"] }),
  grounded({ slug: "no-hallucination-layer", name: "The No-Hallucination Layer", capability: "The grounding guarantee", relatedKeys: ["assistant_capability:evidence-chains", "assistant_capability:provenance-aware-answers"], description: "The design principle beneath the assistant: it surfaces only facts already in the knowledge graph, each with its provenance and a traceable chain of relations. A future language model would phrase these grounded facts, never add to them — so an answer can always be checked against the graph.", highlights: ["Only real facts, always checkable"] }),

  // Architecture — interfaces prepared for a future LLM layer
  arch({ slug: "answer-modes", name: "Answer Modes", altNames: ["Educational, research & expert modes"], capability: "Architecture-ready audience modes", relatedKeys: ["assistant_capability:scientific-summaries", "assistant_capability:no-hallucination-layer"], description: "An architecture-ready interface for tuning the depth of an answer to its audience — an educational mode for newcomers, a research mode for depth, an expert mode for concision. The modes are defined; a language-model layer would realise them over the same grounded facts.", highlights: ["Educational, research & expert — interface ready"] }),
  arch({ slug: "rag-ready-interfaces", name: "RAG-Ready Interfaces", capability: "Retrieval-augmented-generation interface", relatedKeys: ["assistant_capability:scientific-search", "assistant_capability:evidence-chains"], description: "The grounded retrieval — search, neighbourhoods, evidence chains — is exposed as a retrieval-augmented-generation interface, so a future language model can ground every response in the real graph. The retrieval is real today; the generation is future work.", highlights: ["Retrieval real today, generation future"] }),
  arch({ slug: "prompt-orchestration", name: "Prompt Orchestration", capability: "Architecture-ready orchestration", relatedKeys: ["assistant_capability:rag-ready-interfaces", "assistant_capability:answer-modes"], description: "An architecture-ready layer for orchestrating a language model over the grounded retrieval — deciding what to fetch, in what order, and how to assemble it. Defined as an interface; no model is wired in yet.", highlights: ["Orchestration interface — model not yet wired"] }),
  arch({ slug: "conversation-memory", name: "Conversation Memory", capability: "Architecture-ready session memory", relatedKeys: ["assistant_capability:prompt-orchestration", "assistant_capability:no-hallucination-layer"], description: "An architecture-ready interface for remembering the thread of a conversation — the entities discussed, the questions asked — so follow-ups stay in context. The interface is prepared; memory stays on the device, private-first, and holds only references into the graph.", highlights: ["Context memory — private-first, interface ready"] }),
  arch({ slug: "llm-integration", name: "LLM Integration", capability: "Architecture-ready model layer", relatedKeys: ["assistant_capability:rag-ready-interfaces", "assistant_capability:no-hallucination-layer"], description: "The seam where a language model would join — phrasing the grounded facts the retrieval layer supplies, always constrained to what the graph can back. The interface is defined; the model is future work, and when it comes it will explain, never invent.", highlights: ["The model seam — defined, not yet filled"] }),
];
