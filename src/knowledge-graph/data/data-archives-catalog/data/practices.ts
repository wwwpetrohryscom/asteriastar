import type { ArchiveRecord } from "@/knowledge-graph/data/data-archives-catalog/types";

/** Open-science practices — the pipelines, identifiers, citation, and reproducibility that make
 *  archived data usable and trustworthy. Each is associated_with the REUSED methods and archives
 *  and the other practices it depends on. */
const pc = (r: Omit<ArchiveRecord, "kind" | "id" | "sources"> & { slug: string; sources?: ArchiveRecord["sources"] }): ArchiveRecord => ({ sources: ["nasa"], ...r, kind: "practice", id: `open_science_practice:${r.slug}` });

export const practices: ArchiveRecord[] = [
  pc({ slug: "data-pipelines-and-calibration", name: "Data Pipelines & Calibration", relatedKeys: ["astronomy_method:error-analysis-and-calibration", "space_telescope:hubble-space-telescope"], description: "The automated processing that turns raw telescope readouts into calibrated, science-ready data — removing instrument signatures, correcting for the atmosphere or detector, and attaching the metadata and error estimates that make the data usable by anyone.", sources: ["nasa"], highlights: ["Raw detector counts become science-ready data"] }),
  pc({ slug: "cross-matching", name: "Cross-Matching", relatedKeys: ["data_archive:vizier", "data_archive:simbad"], description: "Identifying the same object across different catalogues and wavelengths — matching a source in an infrared survey to its optical and X-ray counterparts — so that all that is known about an object can be brought together despite different names and positions.", sources: ["nasa"] }),
  pc({ slug: "the-ads-literature-service", name: "The ADS Literature Service", altNames: ["Astrophysics Data System", "ADS"], operatorLabel: "Operated by SAO for NASA", relatedKeys: ["organization:sao", "organization:nasa"], description: "NASA's Astrophysics Data System, the digital library of astronomy — indexing essentially the entire research literature and linking every paper to the data, catalogues, and objects it uses, so the literature and the data are one connected web.", sources: ["nasa"], highlights: ["The digital library that indexes all of astronomy"] }),
  pc({ slug: "persistent-identifiers", name: "Persistent Identifiers", altNames: ["DOI", "ORCID", "bibcodes"], relatedKeys: ["open_science_practice:the-ads-literature-service", "data_archive:ned"], description: "The stable, unique names that make science findable and creditable — DOIs for datasets and papers, ORCID iDs for researchers, and bibcodes for the literature — so a result can be cited, retrieved, and attributed unambiguously years later.", sources: ["nasa"] }),
  pc({ slug: "reproducibility-and-fair", name: "Reproducibility & FAIR Data", relatedKeys: ["open_science_practice:persistent-identifiers", "data_standard:fits"], description: "The principles that make science trustworthy and reusable — data that is Findable, Accessible, Interoperable, and Reusable (FAIR), open archives, documented pipelines, and citable datasets — so that a result can be checked and built upon by anyone.", sources: ["nasa"], highlights: ["Findable, Accessible, Interoperable, Reusable"] }),
];
