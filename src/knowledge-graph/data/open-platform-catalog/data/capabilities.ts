import type { PlatformCapabilityRecord } from "@/knowledge-graph/data/open-platform-catalog/types";

/** Open Astronomy Platform capabilities (Program BX). `available` capabilities work today over the real
 *  graph; `architecture-ready` ones have a defined interface but serve no data yet — never faked. */
const cap = (r: Omit<PlatformCapabilityRecord, "id" | "sources"> & { slug: string; sources?: PlatformCapabilityRecord["sources"] }): PlatformCapabilityRecord => ({
  sources: ["nasa"],
  ...r,
  id: `platform_capability:${r.slug}`,
});

export const capabilities: PlatformCapabilityRecord[] = [
  // ---- Public API (available) ----
  cap({
    slug: "public-graph-api",
    name: "Public Graph API",
    category: "api",
    status: "available",
    standard: "REST / JSON",
    endpoint: "/api/v0/entities",
    description: "A read-only, deterministic REST API over the whole knowledge graph — list and fetch entities, list relationships, search, and traverse neighbourhoods and shortest paths. Every response carries a provenance envelope (version, source, licence, attribution). No auth, no rate limits, no write endpoints.",
    relatedKeys: ["graph_view:graph-api", "platform_capability:openapi-spec", "platform_capability:graph-neighbours-path", "platform_capability:sources-api"],
    highlights: ["The whole graph, read-only, with provenance on every response"],
  }),
  cap({
    slug: "graph-neighbours-path",
    name: "Neighbours & Evidence Paths",
    category: "api",
    status: "available",
    standard: "REST / JSON",
    endpoint: "/api/v0/traversal",
    description: "The graph query surface: breadth-first neighbourhood traversal and shortest evidence paths between two entities, cycle-safe and bounded. The same real algorithms behind the graph explorer and the grounded assistant, exposed as an API.",
    relatedKeys: ["platform_capability:public-graph-api"],
    highlights: ["Real BFS neighbours & shortest paths"],
  }),
  cap({
    slug: "openapi-spec",
    name: "OpenAPI Specification",
    category: "api",
    status: "available",
    standard: "OpenAPI 3.1",
    endpoint: "/api/v0/openapi.json",
    description: "A machine-readable OpenAPI 3.1 description of every implemented endpoint, generated from the endpoint registry — so a client, a code generator, or an agent can discover the API automatically.",
    relatedKeys: ["platform_capability:public-graph-api", "platform_capability:sdk-python"],
    highlights: ["Auto-generated, always in sync with the endpoints"],
  }),
  cap({
    slug: "sources-api",
    name: "Sources API",
    category: "api",
    status: "available",
    standard: "REST / JSON",
    endpoint: "/api/v0/sources",
    description: "The registry of authoritative sources behind the platform — every source's name, organisation, canonical URL, licence, and scope — served as a public endpoint, so the provenance of any cited fact can be resolved programmatically.",
    relatedKeys: ["platform_capability:licensing-matrix", "platform_capability:citation-export"],
    highlights: ["Every source, its licence & scope — programmatically"],
  }),
  cap({
    slug: "citation-export",
    name: "Citation Export",
    category: "api",
    status: "available",
    standard: "APA · BibTeX · RIS",
    endpoint: "/api/v0/citations",
    description: "The real, source-backed citations behind entities, served as an API and exportable in APA, Chicago, MLA, Harvard, BibTeX, and RIS through the platform's citation engine. Every field comes from a verifiable citation record; none is invented.",
    relatedKeys: ["platform_capability:sources-api"],
    highlights: ["Real citations in every standard style"],
  }),

  // ---- Graph exports (available) ----
  cap({
    slug: "graph-json",
    name: "Knowledge Graph Export (JSON)",
    category: "graph",
    status: "available",
    standard: "JSON",
    endpoint: "/data/graph.json",
    description: "The complete typed graph — every entity and relation — as a single JSON document, generated from the real graph. The exact bytes served back the download manifest's size and checksum.",
    relatedKeys: ["platform_capability:graph-jsonld", "platform_capability:bulk-downloads"],
    highlights: ["The whole graph in one real, checksummed file"],
  }),
  cap({
    slug: "graph-jsonld",
    name: "Knowledge Graph Export (JSON-LD / RDF)",
    category: "graph",
    status: "available",
    standard: "JSON-LD 1.1 / RDF",
    endpoint: "/data/graph.jsonld",
    description: "The graph as JSON-LD — an RDF-compatible, SPARQL-ready document with a resolvable @id per node and relations as predicates. The linked-data foundation the SPARQL and federation layers are built to sit on.",
    relatedKeys: ["platform_capability:graph-json", "platform_capability:sparql-endpoint", "platform_capability:federation-metadata"],
    highlights: ["RDF-compatible linked data, ready for SPARQL"],
  }),

  // ---- Datasets (available) ----
  cap({
    slug: "dataset-exports",
    name: "Dataset Exports",
    category: "datasets",
    status: "available",
    standard: "JSON · CSV",
    endpoint: "/api/v0/datasets",
    description: "Every curated dataset — from named stars to live-provider status to 3D scenes — exportable as JSON and CSV with real record counts, licences, and provenance. A focused slice of the graph for a specific need.",
    relatedKeys: ["platform_capability:public-graph-api", "platform_capability:bulk-downloads"],
    highlights: ["Focused JSON/CSV slices with real counts"],
  }),

  // ---- Downloads & releases (available) ----
  cap({
    slug: "bulk-downloads",
    name: "Bulk Downloads",
    category: "downloads",
    status: "available",
    standard: "SHA-256 checksummed",
    endpoint: "/open-platform/downloads",
    description: "The full graph exports offered as bulk downloads, each with a REAL byte size and SHA-256 checksum computed from the exact bytes served, plus its licence and release. No download is fabricated — the checksum can be verified against the file.",
    relatedKeys: ["platform_capability:graph-json", "platform_capability:versioned-releases", "platform_capability:doi-releases"],
    highlights: ["Real, verifiable SHA-256 checksums — nothing faked"],
  }),
  cap({
    slug: "versioned-releases",
    name: "Versioned Releases",
    category: "downloads",
    status: "available",
    standard: "SemVer (graph & schema)",
    endpoint: "/api/v0/openapi.json",
    description: "The graph and its schema carry versions, surfaced in every API response's provenance envelope, in the OpenAPI description, and on every export — so a consumer can pin a release and know when entities or the schema change.",
    relatedKeys: ["platform_capability:bulk-downloads", "platform_capability:doi-releases"],
    highlights: ["Pin a release; provenance on every response"],
  }),

  // ---- Licensing (available) ----
  cap({
    slug: "licensing-matrix",
    name: "Licensing Matrix",
    category: "licenses",
    status: "available",
    standard: "CC BY-SA 4.0 + per-source",
    endpoint: "/open-platform/licenses",
    description: "A clear matrix of the licences that apply — the platform's own CC BY-SA 4.0 for the graph and API, and the individual terms of each upstream source (NASA, ESA, IAU, and the rest) — so reuse obligations are never in doubt.",
    relatedKeys: ["platform_capability:sources-api"],
    highlights: ["Platform & per-source terms, side by side"],
  }),

  // ---- Standards (architecture-ready) ----
  cap({
    slug: "sparql-endpoint",
    name: "SPARQL Endpoint",
    category: "standards",
    status: "architecture-ready",
    standard: "SPARQL 1.1",
    description: "A hosted SPARQL query endpoint over the graph's RDF. The linked-data shape is live in the JSON-LD export; a queryable SPARQL service is the architecture-ready next step and is not yet hosted — no endpoint is advertised as live.",
    limitations: "Not yet hosted. The JSON-LD/RDF export exists and can be loaded into any triple store today; a public SPARQL endpoint is architecture-ready, not live.",
    relatedKeys: ["platform_capability:graph-jsonld", "platform_capability:graphql-api"],
    highlights: ["RDF is live; the hosted query service is next"],
  }),
  cap({
    slug: "graphql-api",
    name: "GraphQL API",
    category: "standards",
    status: "architecture-ready",
    standard: "GraphQL",
    description: "A GraphQL surface over the same graph, letting a client request exactly the entities, fields, and relations it needs in one query. The schema maps cleanly onto the typed graph; the resolver layer is architecture-ready and not yet served.",
    limitations: "Not yet served. The REST API delivers the same data today; the GraphQL schema and resolvers are architecture-ready.",
    relatedKeys: ["platform_capability:public-graph-api", "platform_capability:sparql-endpoint"],
    highlights: ["Ask for exactly what you need — interface ready"],
  }),
  cap({
    slug: "doi-releases",
    name: "DOI-Minted Releases",
    category: "downloads",
    status: "architecture-ready",
    standard: "DataCite DOI",
    description: "Citable, DOI-minted snapshots of the graph, so a specific release can be referenced in the literature. The versioning and checksummed exports that a DOI release needs are live; minting DOIs through a registration agency is architecture-ready.",
    limitations: "No DOI is minted yet. Versioned, checksummed releases exist; DOI registration via a DataCite member is architecture-ready, not done.",
    relatedKeys: ["platform_capability:versioned-releases", "platform_capability:bulk-downloads"],
    highlights: ["Checksummed releases now; citable DOIs next"],
  }),

  // ---- SDKs (architecture-ready) ----
  cap({
    slug: "sdk-python",
    name: "Python SDK",
    category: "sdk",
    status: "architecture-ready",
    standard: "Python client",
    description: "A Python client over the public API — typed models for entities and relations, and helpers for search, traversal, and export. The API and its OpenAPI description are live, so a client can be generated today; a curated, published package is architecture-ready.",
    limitations: "No package is published yet. The OpenAPI 3.1 spec supports client generation now; a curated Python SDK is architecture-ready.",
    relatedKeys: ["platform_capability:openapi-spec", "platform_capability:sdk-js"],
    highlights: ["Generate a client from OpenAPI today"],
  }),
  cap({
    slug: "sdk-js",
    name: "JavaScript / TypeScript SDK",
    category: "sdk",
    status: "architecture-ready",
    standard: "JS / TS client",
    description: "A typed JavaScript/TypeScript client over the public API, for the browser or Node. As with the Python SDK, the OpenAPI description makes a client generatable today; a curated, published package is architecture-ready.",
    limitations: "No package is published yet. Client generation from the OpenAPI spec works now; a curated JS/TS SDK is architecture-ready.",
    relatedKeys: ["platform_capability:openapi-spec", "platform_capability:sdk-python"],
    highlights: ["Typed API client for browser & Node — interface ready"],
  }),

  // ---- Federation (architecture-ready) ----
  cap({
    slug: "federation-metadata",
    name: "Federation-Ready Metadata",
    category: "federation",
    status: "architecture-ready",
    standard: "Linked-data IRIs",
    description: "Every entity already carries a resolvable IRI in the JSON-LD export, the foundation for federating the graph with other linked-data sources. A published federation manifest and cross-dataset link set are architecture-ready.",
    limitations: "Resolvable IRIs exist in the JSON-LD; a formal federation manifest and external owl:sameAs link set are architecture-ready, not yet published.",
    relatedKeys: ["platform_capability:graph-jsonld", "platform_capability:vo-tap-adql"],
    highlights: ["Resolvable IRIs now; a federation manifest next"],
  }),
  cap({
    slug: "vo-tap-adql",
    name: "Virtual Observatory (TAP / ADQL)",
    category: "federation",
    status: "architecture-ready",
    standard: "IVOA TAP / ADQL",
    description: "An interface prepared for the International Virtual Observatory Alliance protocols — a TAP service answering ADQL queries over the tabular catalogues (stars, deep-sky, exoplanets). The catalogues carry the real measured columns; a TAP/ADQL service over them is architecture-ready.",
    limitations: "No TAP service is hosted. The catalogues hold the real columns (RA/Dec, magnitudes, distances); a TAP/ADQL endpoint is architecture-ready, not live.",
    relatedKeys: ["platform_capability:federation-metadata", "platform_capability:dataset-exports"],
    highlights: ["Prepared for VO interoperability — not yet hosted"],
  }),
];
