import type { GraphViewRecord } from "@/knowledge-graph/data/graph-explorer-catalog/types";

/** Graph-explorer views. The computed views run real algorithms over the actual graph; each links to
 *  the reused example entities it demonstrates and to sibling views. */
const view = (r: Omit<GraphViewRecord, "id" | "sources"> & { slug: string; sources?: GraphViewRecord["sources"] }): GraphViewRecord => ({ sources: ["nasa"], ...r, id: `graph_view:${r.slug}` });

export const views: GraphViewRecord[] = [
  view({ slug: "graph-statistics", name: "Graph Statistics", backing: "computed", capability: "Live counts over the real graph", relatedKeys: ["graph_view:knowledge-metrics", "graph_view:taxonomy-explorer"], description: "The knowledge graph in numbers, counted live from the real data: how many entities and relations it holds, how they split across science, culture, and astrology, which entity types are largest, and which entities are the most connected hubs.", highlights: ["Every number counted from the real graph"] }),
  view({ slug: "knowledge-metrics", name: "Knowledge Metrics", backing: "computed", capability: "Degree distribution & connectivity", relatedKeys: ["graph_view:graph-statistics", "graph_view:force-directed-graph"], description: "Deeper measures of the graph's structure — the average number of links per entity, the distribution of connectivity, and the hubs that hold the network together. The shape of the knowledge, not just its size.", highlights: ["Average degree, hubs & connectivity"] }),
  view({ slug: "entity-explorer", name: "Entity Explorer", backing: "computed", capability: "Browse every entity by type & domain", relatedKeys: ["graph_view:neighborhood-explorer", "graph_view:graph-search"], description: "A browsable index of every entity in the graph, organised by type and domain, each a gateway into its neighbourhood and its place in the wider web of knowledge.", highlights: ["Every entity, by type & domain"] }),
  view({ slug: "relation-explorer", name: "Relation Explorer", backing: "computed", capability: "Browse the relation types", relatedKeys: ["graph_view:entity-explorer", "graph_view:taxonomy-explorer"], description: "The verbs of the graph — the relation types that connect entities, from 'orbits' and 'discovered by' to 'associated with' — and how often each is used. The grammar of the knowledge.", highlights: ["The relation types & their frequencies"] }),
  view({ slug: "neighborhood-explorer", name: "Neighbourhood Explorer", backing: "computed", capability: "Breadth-first neighbourhood expansion", relatedKeys: ["star:sirius", "graph_view:shortest-path-finder"], description: "The immediate and extended neighbourhood of any entity, expanded breadth-first over the real relations. Start at a star and watch its constellations, catalogues, and physics unfold around it.", highlights: ["Real BFS neighbourhood expansion"] }),
  view({ slug: "shortest-path-finder", name: "Shortest-Path Finder", backing: "computed", capability: "Breadth-first shortest path", relatedKeys: ["star:sirius", "galaxy:milky-way"], description: "The shortest chain of relations connecting any two entities, found by breadth-first search over the real graph. Every link in the path is a genuine relation — a demonstration of how tightly the knowledge is woven together.", highlights: ["A real chain of relations between any two entities"] }),
  view({ slug: "taxonomy-explorer", name: "Taxonomy Explorer", backing: "computed", capability: "The entity-type hierarchy", relatedKeys: ["graph_view:relation-explorer", "graph_view:cross-domain-explorer"], description: "The graph's schema laid bare — the entity types and how they connect to one another, from stars and galaxies to missions and methods. The scaffolding on which every entity hangs.", highlights: ["The types & how they link"] }),
  view({ slug: "cross-domain-explorer", name: "Cross-Domain Explorer", backing: "computed", capability: "Links spanning science, culture & astrology", relatedKeys: ["constellation:orion", "graph_view:taxonomy-explorer"], description: "The relations that reach across the graph's domains — where the science of the sky meets its cultural and historical meaning, as when a constellation joins its stars to its myths. The threads that make the graph one whole rather than three.", highlights: ["Where the domains meet"] }),
  view({ slug: "force-directed-graph", name: "Force-Directed Graph", backing: "rendering", capability: "Physics-laid-out node-link view", relatedKeys: ["graph_view:neighborhood-explorer", "graph_view:cluster-visualization"], description: "A node-link visualisation in which entities repel and relations pull, so the graph settles into a shape that reveals its clusters and hubs. A rendering mode over the real neighbourhood data.", highlights: ["Clusters & hubs, laid out by physics"] }),
  view({ slug: "hierarchical-graph", name: "Hierarchical Graph", backing: "rendering", capability: "Layered / tree layout", relatedKeys: ["graph_view:taxonomy-explorer", "graph_view:scientific-lineage"], description: "A layered layout that arranges entities by level — a taxonomy from the top down, or a dependency chain from cause to effect. A rendering mode for the graph's hierarchical structure.", highlights: ["Top-down layered layout"] }),
  view({ slug: "cluster-visualization", name: "Cluster Visualisation", backing: "rendering", capability: "Community / cluster grouping", relatedKeys: ["graph_view:force-directed-graph", "graph_view:knowledge-metrics"], description: "A grouping of the graph into its natural communities — the tightly-linked clusters of related entities, such as a mission and its instruments, or a star and its planets. A rendering mode over the real link structure.", highlights: ["The graph's natural communities"] }),
  view({ slug: "mission-graph", name: "Mission Graph", backing: "computed", capability: "A mission and everything it touches", relatedKeys: ["space_telescope:hubble-space-telescope", "graph_view:neighborhood-explorer"], description: "The web around a single mission — its instruments, targets, discoveries, and the institutions behind it — drawn from the real relations. A lens that turns a mission from a name into a network.", highlights: ["A mission as a network"] }),
  view({ slug: "institution-graph", name: "Institution Graph", backing: "computed", capability: "An institution and its work", relatedKeys: ["organization:jpl", "graph_view:mission-graph"], description: "The reach of a research institution across the graph — the missions it runs, the facilities it operates, and the discoveries it has enabled — traced through the real relations.", highlights: ["An institution's reach"] }),
  view({ slug: "discovery-graph", name: "Discovery Graph", backing: "computed", capability: "A discovery in context", relatedKeys: ["historical_discovery:pulsars", "graph_view:scientific-lineage"], description: "A discovery set in its web of context — the people who made it, the instruments that enabled it, the objects it revealed, and the ideas it changed. The story of a discovery as a graph.", highlights: ["A discovery in its full context"] }),
  view({ slug: "scientific-lineage", name: "Scientific Lineage", backing: "computed", capability: "Intellectual lineage & influence", relatedKeys: ["astronomer:johannes-kepler", "astronomical_theory:keplers-laws"], description: "The intellectual lineage running through the graph — how an astronomer's work connects to the theories, laws, and later discoveries that built on it. The descent of ideas, traced through the real relations.", highlights: ["The descent of ideas"] }),
  view({ slug: "graph-search", name: "Graph Search", backing: "computed", capability: "Search every entity by name", relatedKeys: ["graph_view:entity-explorer", "graph_view:neighborhood-explorer"], description: "A direct search across every entity in the graph by name — the fastest way from a word to a node and its neighbourhood.", highlights: ["From a word to a node"] }),
  view({ slug: "graph-api", name: "Graph API", backing: "architecture", capability: "Architecture-ready programmatic access", relatedKeys: ["graph_view:graph-statistics", "graph_view:entity-explorer"], description: "An architecture-ready interface for programmatic access to the graph — entities, relations, neighbourhoods, and paths — so other tools can build on the knowledge. The interface is defined; a public endpoint is future work.", highlights: ["Programmatic access — interface ready"] }),
];
