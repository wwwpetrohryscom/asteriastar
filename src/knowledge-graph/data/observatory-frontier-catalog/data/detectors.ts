import type { FrontierRecord } from "@/knowledge-graph/data/observatory-frontier-catalog/types";

/** Detector technologies — how photons are turned into measured signal, from the optical CCD to
 *  the superconducting detectors of the millimetre and submillimetre sky. Each links to the REUSED
 *  wavelength bands and methods it serves. */
const det = (r: Omit<FrontierRecord, "kind" | "id" | "sources"> & { slug: string; sources?: FrontierRecord["sources"] }): FrontierRecord => ({ sources: ["noirlab"], ...r, kind: "detector", id: `detector_technology:${r.slug}` });

export const detectors: FrontierRecord[] = [
  det({ slug: "ccd", name: "CCD", altNames: ["Charge-Coupled Device"], relatedKeys: ["astronomy_method:photometry", "wavelength_band:visible-light", "observatory:vera-rubin-observatory"], description: "The charge-coupled device, the detector that transformed optical astronomy — a silicon chip that converts light into electric charge with high efficiency and low noise, read out pixel by pixel. It replaced photographic plates and remains the standard optical detector; Rubin's camera is built from a mosaic of them.", sources: ["noirlab"], highlights: ["The chip that replaced the photographic plate"] }),
  det({ slug: "cmos", name: "CMOS Sensor", altNames: ["Complementary Metal-Oxide-Semiconductor"], relatedKeys: ["astronomy_method:photometry", "wavelength_band:visible-light"], description: "A detector in which each pixel has its own amplifier, allowing very fast, low-power readout. Increasingly used in astronomy for high-speed imaging and for techniques that need many frames per second, and ubiquitous in consumer cameras.", sources: ["noirlab"] }),
  det({ slug: "mkid", name: "MKID", altNames: ["Microwave Kinetic Inductance Detector"], relatedKeys: ["wavelength_band:submillimeter", "wavelength_band:millimeter", "astronomy_method:spectroscopy"], description: "A superconducting detector that records not just the arrival of a photon but its energy and precise time, with no read noise. Operating at temperatures near absolute zero, MKIDs are a frontier technology for the millimetre, submillimetre, and optical sky, and scale to large arrays.", sources: ["noirlab"] }),
  det({ slug: "bolometer", name: "Bolometer", relatedKeys: ["wavelength_band:submillimeter", "wavelength_band:infrared", "telescope:south-pole-telescope"], description: "A detector that measures light by the tiny rise in temperature it causes in an absorbing element — the standard way to detect the faint millimetre and submillimetre sky, including the cosmic microwave background. Cooled to a fraction of a degree above absolute zero; the South Pole Telescope's cameras use large bolometer arrays.", sources: ["noirlab"] }),
  det({ slug: "cryogenic-detector", name: "Cryogenic Detector", relatedKeys: ["detector_technology:bolometer", "detector_technology:mkid", "wavelength_band:submillimeter"], description: "The broad family of detectors that must be cooled to cryogenic temperatures — sometimes thousandths of a degree above absolute zero — so that thermal noise does not swamp the faint astronomical signal. Bolometers and MKIDs are cryogenic detectors; the technique is essential across the infrared, millimetre, and X-ray.", sources: ["noirlab"] }),
];
